# Quiz 3 - Clave de Respuestas

| Pregunta | Respuesta | Explicación breve |
|----------|-----------|-------------------|
| 1 | B | El bucle se ejecuta 2 veces: primera iteración (total=25), segunda iteración (total=50), luego la condición `50 < 50` es falsa y termina. |
| 2 | C | La condición del `while` determina si el bucle continúa ejecutándose. |
| 3 | A | `coin` vale 10, que es uno de los valores válidos, por lo tanto imprime "Valid". |
| 4 | B | El valor 5 no está en la condición del `if`, entonces no se suma al total y el bucle nunca alcanza el `goal`. |
| 5 | B | Cada comparación debe ser explícita usando `==` y conectadas con `or`. |
| 6 | C | Los números se agregan en orden de entrada: primero 10, luego 20, luego 30. |
| 7 | C | Sin incrementar `i`, la condición `i < 5` siempre es verdadera (0 < 5), creando un bucle infinito. |
| 8 | B | Como `total` (75) no es igual a `goal` (50), entra al `else` y calcula el cambio: 75 - 50 = 25. |
| 9 | B | `if` evalúa una vez; `while` repite mientras la condición sea verdadera. |
| 10 | C | La suma de todos los elementos: 10 + 20 + 5 + 15 + 30 = 80. |
| 11 | C | Para continuar "hasta que" total >= goal, el bucle debe ejecutarse "mientras" total < goal. |
| 12 | B | Para encontrar el mínimo, actualizamos cuando encontramos un número menor que el mínimo actual. |